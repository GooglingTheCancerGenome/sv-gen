# I/O files
input:
  fasta: data/chr17.fasta  # reference genome (haploid)
  # chr: 17
  config: params.txt       # SURVIVOR config file
output:
  fasta:
    hmz: data/hmz/chr17.fasta      # diploid reference (homozygous)
    hmzsv: data/hmzsv/chr17.fasta  # diploid genome with SVs (homozygous)
    htzsv: data/htzsv/chr17.fasta  # diploid genome with SVs (heterozygous)

# I/O files (extensions) used by the workflow
# file_exts:
#   fasta: .fasta
#   fasta_idx:
#     - .fasta.fai  # faidx index file
#     - .fasta.bwt  # BWA v0.6.x index files
#     - .fasta.amb  # |
#     - .fasta.ann  # |
#     - .fasta.pac  # |
#     - .fasta.sa   # |
#   bam: .bam
#   bam_idx: .bam.bai
#   vcf: .vcf
#   bed: .bed
#   fastq: .fq  # temp files 

#sim_genomes:
#
sim_reads:
  seed: 1000
  stdev: 10
  # min: 300
  # max: 500
  # step: 50  
  read_len: 150
  # min: 300
  # max: 500
  # step: 50
  profile: HSXt
  coverage: 30 # max. coverage; use samtools/sambamba to downsample
  # min: 10
  # max: 30
  # step: 5
  insert_size: 500
  # min: 300
  # max: 500
  # step: 50
